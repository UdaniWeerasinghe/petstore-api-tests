# API Configuration
base.url=https://petstore.swagger.io/v2
api.key=special-key

# Connection settings
connection.timeout=30000
socket.timeout=30000

# Logging
log.all=true

# Retry configuration
retry.count=3
